# Base URL of the FastAPI backend, without a trailing slash.
# Local development reads .env.development; on Vercel, set this under
# Project → Settings → Environment Variables (it is inlined at build time).
API_URL=https://api.example.com
