# Backend used by the deployed frontend (Vercel inlines this at build time).
# Override it in Vercel → Project → Settings → Environment Variables if the
# backend ever moves.
VITE_API_URL=https://datafuse-ai-backend.vercel.app
